{% extends 'basef.html.twig' %}

{% block title %}Creer caracteristique!{% endblock %}

{% block body %}

<div class="add_utf_listing_section margin-top-45 w-50">
    <div class="utf_add_listing_part_headline_part">
        <h3><i class="sl sl-icon-tag"></i> Carateristiques sportif</h3>
    </div>
    {{ form_start(f,{attr:{novalidate:'novalidate'}}) }}
                    <label for="inputText" class="col-sm-4 col-form-label">Taille</label>
                    <div class="col-sm-10">
                        {{ form_widget(f.taille,{'attr':{'class':'form-control'}}) }}
                    </div>
                    {{ form_errors(f.taille,{'attr':{'class':'text-danger'}}) }}

                    <label for="inputText" class="col-sm-4 col-form-label">Genre</label>
                    <div class="col-sm-10">
                        {{ form_widget(f.genre,{'attr':{'class':'form-control'}}) }}
                    </div>
                    {{ form_errors(f.genre,{'attr':{'class':'text-danger'}}) }}

                    <label for="inputText" class="col-sm-4 col-form-label">Poids</label>
                    <div class="col-sm-10">
                        {{ form_widget(f.poids,{'attr':{'class':'form-control'}}) }}

                    </div>
                    {{ form_errors(f.poids,{'attr':{'class':'text-danger'}}) }}
                    <label for="inputText" class="col-sm-4 col-form-label">Age</label>
                    <div class="col-sm-10">
                        {{ form_widget(f.age,{'attr':{'class':'form-control'}}) }}
                    </div>
                    {{ form_errors(f.age,{'attr':{'class':'text-danger'}}) }}

<!---->
                    <label for="inputText" class="col-sm-4 col-form-label">Etes-vous un robot ?</label>
                    <div class="col-sm-10">
                        {{ form_widget(f.captchaCode,{'attr':{'class':'form-control'}}) }}
                    </div>
                    {{ form_errors(f.age,{'attr':{'class':'text-danger'}}) }}


                    <!---->

                    <br/>
                    <div class="col-sm-10" style="display: flex;align-items: center;justify-content: center">

                        {{ form_widget(f.Ajouter,{'attr':{'class':'btn btn-primary'}}) }}
                    </div>

                    {{ form_end(f) }}

</div>


{% endblock %}
