{% extends 'base.html.twig' %}

{% block title %}New Regime{% endblock %}

{% block body %}
    <div class="superx">
    <h1>Create new Regime</h1>

    {{ include('regime/_form.html.twig') }}

        <a  type="button" class="btn btn-primary" href="{{ path('app_regime_index') }}">back to list</a>
    </div>
{% endblock %}
